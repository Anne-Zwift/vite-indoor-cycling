/**
 *  The state object is defined here.
 * (eg. state = { isLoggedIn: false, userProfile: null })
 */

export const state = { 
  isLoggedIn: false, 
  userProfile: null as { name: string } | null, 
};